module.exports = {
  /**
   * @전체게시글_조회하기
   * @route GET ~/post
   * @access public
   * @err
   */
  getAllPost: async (res, req) => {
    try {
    } catch (error) {}
  },

  /**
   * @게시글_작성하기
   * @route POST ~/post
   * @access public
   * @err
   */
  createPost: async (res, req) => {
    try {
    } catch (error) {}
  },
};
